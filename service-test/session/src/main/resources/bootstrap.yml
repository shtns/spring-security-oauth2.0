server:
  port: 8111

spring:
  application:
    name: @project.artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        namespace: test
        group: TEST_GROUP
  profiles:
    active: @profiles.active@

